import { BANKING_NOTICE,maskDemoIban,type DemoBanking } from "@/lib/domain/demoBanking";
export function BankingSummary({banking}:{banking?:DemoBanking}) {
 return <section className="m-panel" aria-label="Coordonnées bancaires"><h2 className="font-semibold">Coordonnées bancaires</h2><p className="m-help">{BANKING_NOTICE}</p>{banking?<dl className="grid gap-2 text-sm sm:grid-cols-2 mt-2"><div><dt>Compte de prélèvement</dt><dd>{banking.paymentAccount.accountHolder} · {maskDemoIban(banking.paymentAccount.iban)}</dd></div><div><dt>Compte de remboursement</dt><dd>{banking.refundAccount.sameAsPayment?"Même compte · ":""}{maskDemoIban(banking.refundAccount.iban)}</dd></div><div><dt>Mode de paiement</dt><dd>{banking.paymentAccount.paymentMethod}</dd></div><div><dt>Statut du mandat</dt><dd>{banking.paymentAccount.mandateStatus}{banking.paymentAccount.mandateDate&&` · ${banking.paymentAccount.mandateDate}`}</dd></div></dl>:<p className="m-help">Non renseignées.</p>}</section>;
}
